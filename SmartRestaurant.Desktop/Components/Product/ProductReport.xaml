<UserControl x:Class="SmartRestaurant.Desktop.Components.Product.ProductReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartRestaurant.Desktop.Components.Product"
             mc:Ignorable="d" 
             Height="60">
    <Border
        Margin="10 4 10 4"
        CornerRadius="5"
        Background="White"
        BorderBrush="Gray"
        BorderThickness="0.5">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="1"
                Margin="10 0 0 0"
                Name="tb_Number"
                Style="{DynamicResource InfoLabel}"/>

            <TextBlock
                Grid.Column="1"
                x:Name="tb_product_name"
                Text="Sho'rva"
                Style="{DynamicResource InfoLabel}"/>

            <TextBlock
                Grid.Column="2"
                Text="26"
                Name="tb_product_count"
                Style="{DynamicResource InfoLabel}"/>

            <TextBlock
                Grid.Column="3"
                Text="279000"
                Name="tb_total_sum"
                Padding="8 0"
                Style="{DynamicResource InfoLabel}"/>

        </Grid>

    </Border>
</UserControl>
